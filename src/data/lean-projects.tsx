export interface LeanProject {
  id: string;
  title: string;
  description: string;
  href?: string;
}

export const leanProjects: LeanProject[] = [
  // Add projects here when they are ready to appear on the page.
];
